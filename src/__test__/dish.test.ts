describe('product', () => {
  describe('get product route', () => {
    describe('given the produst does not exist', () => {
      it('should return a 404', () => {
        expect(true).toBe(true);
      });
    });
  });
});
